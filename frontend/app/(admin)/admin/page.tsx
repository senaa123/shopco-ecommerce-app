/**
 * Admin dashboard home.
 *
 * Placeholder — real dashboard widgets are built in a later prompt.
 */
export default function AdminDashboardPage() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center gap-2 p-16 text-center">
      <h1 className="text-2xl font-semibold tracking-tight text-foreground">
        ShopCo Admin
      </h1>
      <p className="text-primary-500">Admin scaffold. Nothing to see here yet.</p>
    </main>
  );
}
